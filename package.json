{
  "name": "smile-memory",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "css:build": "npx @tailwindcss/cli -i static/src/input.css -o static/css/output.css --minify",
    "css:watch": "npx @tailwindcss/cli -i static/src/input.css -o static/css/output.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4",
    "@tailwindcss/typography": "^0.5.19"
  }
}
